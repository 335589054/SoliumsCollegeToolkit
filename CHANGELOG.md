# Uni ToolKit — 更新记录（Changelog）

> 面向大学生的一站式便捷工具 APP 的版本更新记录
> 最后更新：2026-09-10

---

## 更新日志

### v1.1.0（2026-09-10）

> 首个正式发布版本。不再发布 v1.0.4（rc 系列），直接从 1.1.0 正式发布；应用**包名由 `com.unitoolkit` 变更为 `com.solium.Unitoolkit`**，旧版本数据不会自动迁移。

**重要提醒：升级安装 v1.1.0 前，请先在旧版本「设置 → 数据管理 → 导出数据」得到 `.stbc` 备份，安装 v1.1.0 后再「导入数据」恢复。**

- 发布：终止 1.0.4 rc 系列，改为发布 **v1.1.0 正式版本**
- 包名：`com.unitoolkit` → **`com.solium.Unitoolkit`**（源码包名 / 命名空间 / applicationId 同步变更；桌面旧应用与数据不再自动迁移，需手动导出 / 导入）
- 设置页自定义主题改为**默认全部折叠**分组展示，界面更清爽
- 数据备份增强：`.stbc` 备份补齐此前遗漏的配置项 —— 课表节次时间预设、首页 / 桌面小组件快捷入口、主题分组顺序、深色模式、学期长度与起始日期、绩点计算公式、番茄钟保持亮屏与沉浸亮度、首页卡片排序等，换机 / 还原后各项设置可完整恢复
- 版本号 v1.1.0（`versionCode = 14`），release 签名构建

### v1.0.4-rc6（2026-09-10）

> 主界面「概览 / 工具箱 / 设置」切换动画改为最原始的左右平移。

- 底部栏 tab（概览 / 工具箱 / 设置）切换动画由「切入」改为**最原始的左右平移**（两页同速平行移动，更自然），方向为：向右切内容整体向左平移，向左切内容整体向右平移
- 子页面进入 / 返回保持 rc5 的水平滑动不变
- 版本号更新为 v1.0.4-rc6（`versionCode = 13`），仍采用 release 构建

### v1.0.4-rc5（2026-09-10）

> 全局页面切换动画统一为水平滑动，并校正各自滑动方向。

- 所有页面切换动画统一为**水平滑动**（不再出现上下方向），并校正方向：
  - 进入子页面（push）：新页面从**右侧**进入、旧页面向**左侧**退出（统一向左推进）
  - 返回（pop）：下层页面从**左侧**进入、当前页面向**右侧**退出（统一向右拉回）
  - 底部栏 tab 切换保持按相对方向滑动（向右切从右侧进入、向左切从左侧进入）
- 版本号更新为 v1.0.4-rc5（`versionCode = 12`），仍采用 release 构建

### v1.0.4-rc4（2026-09-10）

> 动效方向修正、主题分组管理增强，首页与桌面小组件快捷入口独立配置并支持排序（补上 rc3 更新日志）。

- 修复：由「设置」切到「工具箱」页面动效方向反转 —— 底部栏 tab 切换改为按相对方向滑动（向右切面板从右侧进入，向左切从左侧进入）
- 主题
  - 设置页自定义主题选择改为**按主题包分组、可折叠**展示（保留主色圆圈 + 名称样式）
  - 管理页支持**新建分组**、**手动把主题移动到某分组**（可在已有分组中选择，或直接输入新分组名）、用 **↑↓ 调整分组顺序**；分组顺序持久化
- 快捷入口：首页快捷入口与桌面「快捷入口」小组件现已**独立配置**，可分别勾选并排序展示顺序（桌面小组件展示最前 4 项）
- 修复：首页快捷入口描述错误（此前文案仍称两者共用）
- 补上 v1.0.4-rc3 的更新日志
- 版本号更新为 v1.0.4-rc4（`versionCode = 11`），仍采用 release 构建

### v1.0.4-rc3（2026-09-10）

> 修复打开「设置」页时崩溃的 bug。

- 修复：打开「设置」页崩溃 —— 自定义主题选择区不再使用嵌套在滚动容器内的无限高度 `LazyVerticalGrid`，改为非懒加载的「主色圆圈 + 主题名称」列表，避免崩溃（保留 rc2 的圆圈 + 名称样式）
- 版本号更新为 v1.0.4-rc3（`versionCode = 10`），仍采用 release 构建

### v1.0.4-rc2（2026-09-10）

> 主题分组管理、备份升级为 .stbc、个人名片优化、文件打开方式绑定与课表节次预设完善。

- 主题
  - 主题包导入时**按文件名自动分组**（去掉扩展名，优先于包内名称）
  - 分组可**管理**：重命名分组、删除分组、将某分组导出为主题包（.sthm）
  - 自定义主题选择 UI 改为**主色圆圈 + 主题名称**，与预设主题风格一致
- 备份升级为 **.stbc**：备份文件**内嵌全部图片资源与主题包**（个人名片头像 / 二维码、悬浮图、主题背景图等均以 base64 内嵌，不再只是图片路径），换机 / 迁移不失效；新格式，暂不支持读取旧备份
- 个人名片：导入二维码时引导**裁剪**；二维码显示适当**放大**；B站 UID 移到**第二行**且不再省略号截断；名片中点击**单项属性可复制**
- 绑定 **.sthm / .stbc** 的系统「打开方式」：从文件管理器打开即可直接导入主题包 / 还原备份
- 修复：导出主题的后缀名从 `.sthm.json` 误跳为**正确的 `.sthm`**（改用自定义文件类型以避免系统追加 .json）
- 课表节次预设
  - 在课程编辑「具体时间」处新增「从节次时间预设快速填写」
  - **清空默认节次预设**（不再默认 10 节），由用户自行配置
  - 原「套用预设」改为**按钮 + 弹窗**选择后再套用
- 版本号更新为 v1.0.4-rc2（`versionCode = 9`），仍采用 release 构建

### v1.0.4-rc1（2026-09-10）

> 主题系统重大升级、日期时间输入优化、课表节次时间预设与多处修复。

- 主题系统重大升级
  - 颜色编辑支持透明度（Alpha）滑杆，浅色 / 深色可独立设置
  - 新增「预览页」，直观显示每个颜色角色分别作用于哪些内容（按钮 / 卡片 / 文字 / 输入框 / 分隔线）
  - 主题新增属性：主页背景图片、番茄钟背景图片、番茄钟沉浸模式背景图片、课表背景图片、课表背景图片透明度、主页卡片全局透明度
  - 背景图片以 base64 直接内嵌到 `.sthm` 主题包（区别于原有仅存「图片路径」的方案，保证换机 / 迁移不失效）；图片仅从 Uri 读取内嵌，导出即可携带
  - `.sthm` 向下兼容：旧主题包缺少新字段时自动取默认值导入
  - 自定义主题支持按「主题包」折叠分组展示，编辑界面整体优化
- 日期 / 时间输入框全局优化：空值自动填充当前日期 / 时间；支持中英文符号及「年月日 / 时分点」汉字分隔；任何符号均可作分隔；日期 4 位数字识别为「月 + 日」、6 / 8 位识别为「年 / 月 / 日」；时间 4 位数字自动加冒号；需同时输入日期时间时拆分为独立两个输入框
- 课表：新增「第几节课时间预设」，可分上 / 下午逐节设置起止时间，与原有课程预设相互独立；时间轴按命中节次套用预设时间显示
- 「下一节课」小组件：修复不主动更新数据的问题——课程数据增删改、备份导入 / 清空、界面编辑后即时刷新小组件
- 工具：修复「绩点计算器」被错误归入生活栏目的问题，移至学习栏目
- 采购清单：删除「采购活动」前增加二次确认
- 主题资源：「爱上雷神」主题包增强紫色元素（主色 / 背景 / 各角色大量紫色），三个变体同步
- 工程规范：AGENTS.md 新增「构建环境」（Gradle 8.7 及路径、AGP / Kotlin / KSP / JDK / SDK 版本）与「变更版本号必须同步『关于』」规则
- 版本号更新为 v1.0.4-rc1（`versionCode = 8`），仍采用 release 构建

### v1.0.4pre1（2026-09-10）

> 修复日程删除与屏幕朝向跳转问题，新增 5 组可手动导入的 `.sthm` 主题包。

- 日程：修复无法删除日程的 bug（Instances 查询中实例 ID 与事件 ID 解析顺序颠倒，导致删除定位到错误行）
- 首页 / 全局：修复屏幕朝向变化（旋转）后自动跳转到课表的 bug（仅冷启动时消费导航路由，配置变更不再重复跳转）
- 主题资源：新增 5 组官方 `.sthm` 主题包（存放于项目 `themes/` 目录，可手动导入）——海洋、天象、植被、高对比度、爱上雷神
- 版本号更新为 v1.0.4pre1（`versionCode = 7`），仍采用 release 构建

### v1.0.3-bugfix1（2026-09-10）

> 紧急修复：课表时间刻度对齐与日程页闪退。

- 课表：三日时间轴的小时刻度与课程开始时间统一对齐刻度线，不再落在刻度之间
- 日程：修复打开日程页闪退（“我的日程”与“系统日历”使用相同列表 key 导致崩溃）
- 日程：移除系统日历列表的强 key，重复 / 周期日程展开后也能正常展示（最终修复）
- 日程：改为直接读写系统日历，不再保留本地“我的日程”，首页日程卡同步显示系统日历
- 日程：新增日程时移除提醒选择，默认无提醒

### v1.0.3（2026-09-10）

> 课表时间轴、自定义颜色、上隐条保存与系统日程读取优化。

- 课表：进入课表默认使用三日时间轴
- 课表：时间轴方块只显示课程名称、开始时间、教室；每小时刻度高度加大，短课程能完整显示
- 自定义主题：颜色选择器改为“色调 + 饱和度 + 亮度”三组滑杆
- 上隐条：离开页面时触发保存，图片已有同名文件时直接替换
- 日程：系统日程改用 Instances 实例查询（覆盖重复/跨时段事件），并保留普通事件表回退
- 首页：日程卡片有内容时保持最小高度，避免与其它卡片长短不一致

### v1.0.2（2026-09-09）

> 细节体验优化：颜色选择、课表星期多选与日历时间轴、采购/账单自动名称、系统日程刷新与上隐条图片持久化修复。

- 自定义主题：每个颜色角色增加颜色选择框，可直接点选颜色，不再只能输入代码
- 课表：上课星期支持多选 / 不选（不选视为每天）
- 课表：新增“保存为预设”按钮，整合到新建 / 编辑课程页，原预设管理入口保留
- 课表：三日时间轴改为系统日历样式：左侧 7:00-22:00 小时刻度，三天并排，课程以彩色方块突出名称与教室
- 采购：新建活动时先选择类型，自动填入“类型 + 当前时间”作为名称；手动改过名称后不再覆盖
- 账单：选择“餐饮”分类时自动填入“日期 + 早饭 / 午饭 / 晚饭 / 夜宵”
- 日程：新增“刷新系统日程”主动读取按钮
- 上隐条：修复重进后仍需重新选择图片的问题；默认高度及高度上限改为原来的一半
- 上隐条：导出数据备份时提示“图片文件无法导出，需要重新选择图片”
- 数据：课表星期多选字段随备份导入导出

### v1.0.1（2026-09-09）

> 界面动效、课表灵活化、上隐条内容类型、自定义主题与个人名片优化。

- 底部菜单与二级页面切换改为水平滑动动画
- 课表：新增三日时间轴 / 日历式布局
- 课表：课程周次支持多选，支持指定日期上课
- 课表：新增自定义预设（名称必填、其余字段可选，禁止完全空白预设），新建课程时可一键套用
- 上隐条：配置与图片保存在应用私有目录，不再每次重新选择
- 上隐条：支持纯色 / 文字内容，图片支持拉伸填充与居中裁切
- 主题系统：新增自定义主题，按 UI 角色字段化编辑浅色 / 深色，支持删除
- 主题系统：支持 .sthm 主题包导入 / 导出（JSON，可包含多个主题）
- 个人名片：B站昵称与 UID 合并展示，新增自定义信息栏
- 数据备份：将个人名片、上隐条、课表预设、番茄钟预设与自定义主题一并纳入 JSON 备份 / 恢复

### v1.0.0（2026-09-09）

> 正式发布版本：完成 release 构建签名，并修复小组件与个人名片图标问题。

- 版本号更新为 v1.0.0，`versionCode = 2`
- 采用 release 方式编译并签名，产物可直接安装
- 桌面「下一节课」与「快捷入口」小组件背景改为半透明圆角
- 修复个人名片中用户图标路径错误导致「头像飞离身体」的问题

**正式签名修订（版本号保持 v1.0.0）**

- 「快捷入口」小组件：图标略微缩小，改为圆角正方形底块内的主题色图标
- 修复部分二级菜单顶部出现大量空白的问题（状态栏安全区被重复叠加）
- 生成正式 keystore（`keystore/release.jks`）并替换 release 签名配置

### v1.0.0-pre1（2026-09-09）

> 应用更名，完善桌面小组件的信息展示与图标，正式进入 1.0.0 预览版阶段。

**名称与版本**

- 应用名称改为「Solium的工具箱」
- 版本号更新为 v1.0.0-pre1（Android `versionName`）

**「下一节课」小组件**

- 新增显示开始时间与上课地点
- 布局优化为「课程名称 + 时间地点」两行圆角卡片
- 点击小组件直接跳转到课表页面

**「快捷入口」小组件**

- 删除全部文字，改为纯图标展示并适当放大图标
- 内置工具显示对应 lucide 图标，自定义链接显示链接图标

**「下一节课」小组件**

- 尺寸调整为 2 宽 × 1 高，使用紧凑布局显示课程、开始时间与地点

**番茄钟通知**

- 手动停止或计时结束后移除前台通知，避免通知栏残留「已停止」通知

**个人名片（生活模块，新增）**

- 新增「个人名片」工具，进入工具箱 → 生活即可查看
- 名片在一屏内根据可用高度合理缩放展示，内容包含头像、姓名、手机号、QQ、QQ / 微信二维码、B站昵称与 UID
- 设置页新增「个人名片信息」，可填写文字信息并选择本地头像 / 二维码图片
- 数据保存在本地 DataStore（`personal_profile`），图片复制到应用私有目录

### v1.0.0-dev6（2026-09-09）

> 新增工程协作规范，重构与新增桌面小组件，并统一应用品牌形象。

**工程规范**

- 新增 AGENTS.md：后续每次开发前先阅读 DEV-DOC.md 与 CHANGELOG.md，修改代码后同步更新两份文档与应用内更新日志

**小组件**

- 「下一节课」小组件改为圆角卡片，移除时间 / 日期 / 标题，只显示下一节课
- 新增 2×2 圆角「快捷入口」小组件：展示设置中勾选的内置工具与便捷链接（最多前 4 项），点击直达对应工具 / 网页
- 「快捷入口」小组件与首页快捷入口共用 `quick_tools` 与 `links.isHomeCard` 配置

**设置**

- 新增「桌面快捷入口小组件」入口，快捷入口管理页同时控制首页卡片与桌面小组件内容
- 修改勾选后即时刷新桌面小组件

**品牌与关于**

- 应用标题改为「工具箱」
- 应用图标改为黑白配色、圆角矩形工具箱图标（含新版系统自适应图标与通知图标）
- 「关于」补充开发者 Solium、邮箱与 B站信息

### v1.0.0-dev5（2026-09-09）

> 本次迭代加入小组件与通知栏能力，优化首页时钟与导航条配色，并增强采购清单、课表、日程。

**通知栏**

- 新增「下一节课」提醒：自动计算最近一节课并推送通知（提前 10 分钟提醒，支持单/双周与仅一次）
- 番茄钟进行中持续在前台通知展示倒计时

**小组件**

- 新增 4×2 桌面小组件（横竖屏通用）：显示时间、日期与下一节课，配色跟随应用主题

**首页**

- 日期 / 时间改为居中时钟样式展示
- 「管理卡片」入口移动至页面底部

**导航条**

- 底部导航条配色改浅（各主题统一调整），修复「水墨」主题下选中图标可读性问题

**采购清单**

- 主界面新增「暂缓采购区」标签页，可查看全部暂缓项目并移回 / 删除

**课表**

- 新增「学期起始」设置：选择第一周开始日期（该周任意日期均可），自动对齐当前周次
- 修复编辑课程时需重新输入全部信息的 bug

**日程**

- 自动读取系统日历事件并展示（与自建日程合并展示，自动去重）

### v1.0.0-dev4（2026-09-09）

> 本次迭代统一全局图标、优化配色主题，并增强采购清单与课表的灵活度。

**采购清单**

- 删除「自定义」类型，新增「奶茶店 / 面包店 / 淘宝网购 / 线下菜市场」四类，并统一各类型图标
- 新增「采购完成」按钮：一键将未购项目移入暂缓采购区，可选输入总金额并同步到账单（支出）

**全局图标**

- 将所有 emoji 图标统一替换为 lucide SVG 图标（导航栏、工具箱、首页卡片、快捷入口、设置、账单、采购、课程、链接、笔记工具栏等），色调统一由主题 `tint` 控制

**配色主题**

- 「外观 · 配色主题」新增「滚动查看更多」描述
- 「爱上雷神」配色改为全局亮黄 + 亮紫
- 底部导航条随主题配色加深

**课表**

- 新增单周 / 双周 / 每周 / 仅一次（指定周次）上课模式
- 新增学期长度设置（周数），周次切换与「仅一次」周次受其约束

**首页**

- 首页快捷入口支持加入全部工具（内置工具默认全量展示，可勾选管理）

### v1.0.0-dev3（2026-09-09）

> 本次迭代新增「采购清单」小工具与数据备份/导入/清空能力，并完善首页卡片显示与快捷入口体验。

**首页**

- 主界面卡片：新增主题色（淡化）背景 + 描边，突出卡片边界，优化显示
- 区分「快捷入口」与「便捷链接」：新增首页快捷入口管理页（工具入口 + 链接入口），修复入口不可点击的问题

**音游上隐条**

- 修复修改宽高后拖动导致设置失效的问题（拖拽位置实时同步）
- 移除「应用设置」按钮，拖动宽高 / 透明度调节条时自动应用到悬浮窗

**采购清单（生活，新增）**

- 按「采购活动」管理清单，支持类型：生活超市 / 朴朴 / 山姆 / 自定义
- 采购项目按 TodoList 方式勾选，新增「暂缓采购」选项，该类项目自动带入下次同类型采购
- 支持导入 txt 文件（提供可复制的模板）

**数据管理（设置，新增）**

- 导出数据：将全部数据备份为纯文本 JSON 文件（SAF）
- 导入数据：支持「增量」与「覆盖」两种模式
- 清空数据：二次确认后删除本地全部数据

**系统**

- 新增「更新日志」页面，按版本查看更新记录

### v1.0.0-dev2（2026-09-09）

> 本次迭代全面重构首页与音游上隐条，扩展主题系统，并完善账单、番茄钟、课表、日期时间的体验。

**音游上隐条（重构）**

- 修正概念：上隐条为遮挡屏幕上方音符、减小读谱压力的图片，而非模拟判定线
- 改为用户自选图片悬浮在屏幕上方
- 可手动调整图片宽度、高度
- 应用外支持拖拽移动（水平、垂直）
- 删除所有预设功能（各音游上隐条一致，无需预设）

**首页（重构）**

- 卡片式布局：圆角卡片、突出主题背景色，卡片内部支持滚动
- 快捷入口定位为「工具箱内工具 + 链接」的聚集地，统一管理
- 新增卡片管理：可添加、删除、移动卡片位置；不允许重复；保存配置
- 新增账单卡：查看当前余额、单日账单、快捷调用工具创建账单（与工具内部数据同步）
- 新增 TodoList 卡：快捷勾选、修改入口直达工具内部（与工具内部数据同步）
- 课表卡片仅显示当天接下来三节课
- 距上课时间 < 10 分钟（核对日期）时，下方显示小字提醒；去掉「当前时间」标签
- 新增日期、星期、农历、节气显示；去掉原应用标题

**番茄钟**

- 修复切换预设时自动开始的 bug
- 新增自定义预设功能（并保存自定义预设）

**外观主题**

- 「黑白」更名为「水墨」
- 优化「爱上雷神」配色，使其更鲜艳、富有冲击感（彩蛋，做特殊标记）
- 新增淡黄、橘色、深蓝等配色，共计 12 套，按色调排序
- 主题列表突出滚动条 / 边框，提示可滚动

**账单**

- 删除增加二次确认（可附谚语）
- 新增一键清除（开发用，同时增加二次确认）
- 按月管理：自动添加当前月份，可查看其他月份账单
- 新增支付方式：微信、支付宝、银行卡、自定义

**课表**

- 增强时间显示的可视化

**全局**

- 日期、时间选择统一调用 Android 系统 API

**基础设施**

- 新增更新日志，支持按版本子菜单查看

### v1.0.0-dev1（2026-09-09）

> 首个 Android 原生交付版本：由 Flutter 全面迁移至 Kotlin + Jetpack Compose，完成三大模块全部工具，并交付可安装 APK。

**总体**

- 全新 Android 原生实现（Kotlin 2.0 + Jetpack Compose + Material 3），替代原 Flutter 版本
- MVVM 架构（ViewModel + Repository），Room + DataStore 本地优先存储，Navigation Compose 声明式路由
- 扁平化 UI 设计，24dp 圆角卡片，深浅色双主题

**学习模块**

- 课表：按具体时间（HH:mm）而非「节次」组织；周视图（周一至周日）切换；增删改查；Markdown / JSON 导出
- 笔记：轻量 Markdown 编辑器（粗体 / 斜体 / 标题 / 列表 / 代码块 / 引用 / 链接）；编辑 / 预览切换；导入导出 `.md`
- TodoList：优先级、标签、截止日期、完成状态、筛选（全部 / 今日到期 / 已完成）
- 番茄钟：自定义时长、预设方案、保持亮屏、沉浸式模式、后台前台服务倒计时、今日专注统计

**生活模块**

- 水电费缴交：缴费网页快捷方式 + 应用内 WebView
- 日程提醒：读写系统日历（CalendarContract），今日剩余日程卡片
- 绩点计算器：自定义公式、学期管理、评分等级预设（4.0 / 5.0 / 自定义）
- 便捷链接收藏：自定义链接、分类管理、首页卡片
- 简易账单：收支记录、月度汇总、分类预设、按日期分组

**娱乐模块**

- 音游上隐条：全屏悬浮窗、多线绘制、全局 / 单线参数、前台服务常驻、预设管理

**交付**

- 构建产品级 debug APK（`app-debug.apk`，约 10.8 MB）；minSdk 24 / targetSdk 35

---

*— END OF CHANGELOG —*